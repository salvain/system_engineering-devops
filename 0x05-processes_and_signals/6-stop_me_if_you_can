#!/usr/bin/env bash
# This script stops the 4-to_infinity_and_beyond process without using kill or killall

PID=$(ps -ef | pgrep '[4]-to_infinity_and_beyond' | awk '{print $2}')

if [ -n "$PID" ]; then
  true > "/proc/$PID/fd/0" 2>/dev/null
fi
